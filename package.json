{
  "name": "@acme/integrations",
  "packageManager": "bun@1.2.17",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "lint": "turbo run lint --continue -- --cache --cache-location .cache/.eslintcache",
    "format": "turbo run format --continue -- --cache --cache-location .cache/.prettiercache",
    "format:fix": "bun run format --write",
    "typecheck": "turbo typecheck",
    "build": "turbo run build",
    "clean": "turbo run clean && git clean -xdf node_modules"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "turbo": "^2.5.4",
    "typescript": "catalog:"
  },
  "trustedDependencies": [
    "@acme/integration-framework"
  ],
  "workspaces": {
    "packages": [
      "integrations/*"
    ],
    "catalog": {
      "@acme/integration-framework": "link:@acme/integration-framework",
      "@acme/style-guide": "link:@acme/style-guide",
      "@types/bun": "^1.2.15",
      "valibot": "^1.1.0",
      "eslint": "^9.29.0",
      "prettier": "^3.5.3",
      "typescript": "^5"
    }
  },
  "patchedDependencies": {
    "cpu-features@0.0.10": "patches/cpu-features@0.0.10.patch"
  }
}
