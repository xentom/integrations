{
  "name": "@acme/integrations-email",
  "displayName": "Email",
  "description": "Send emails using SMTP with nodemailer. Configure your email server and send professional emails through workflows.",
  "version": "0.0.5",
  "type": "module",
  "icon": "./images/icon.png",
  "main": "./build/index.js",
  "scripts": {
    "lint": "eslint",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "typecheck": "tsc --noEmit",
    "clean": "git clean -xdf .cache .turbo node_modules build dist",
    "dev": "acme dev",
    "build": "acme build",
    "publish": "acme publish"
  },
  "dependencies": {
    "@acme/integration-framework": "catalog:",
    "nodemailer": "^6.9.8",
    "valibot": "catalog:"
  },
  "devDependencies": {
    "@acme/style-guide": "catalog:",
    "@types/bun": "catalog:",
    "@types/nodemailer": "^6.4.14",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:"
  },
  "prettier": "@acme/style-guide/prettier"
}
